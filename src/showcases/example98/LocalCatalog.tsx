import type { Product } from "../../types/showcase";
type Props = { products: Product[]; slug: string; navigate: (path: string) => void; whatsapp?: string; purchaseLabel: string; detailLabel: string; contactPrefix: string; locale: string };
export default function LocalCatalog({ products, slug, navigate, whatsapp, purchaseLabel, detailLabel, contactPrefix, locale }: Props) { return <div className="e98-catalog-grid">{products.filter(({ hide }) => !hide).map((product) => { const message = encodeURIComponent(`${contactPrefix} ${product.name}.`); return <article className="e98-card" key={product.number}><button className="e98-card-image" type="button" onClick={() => navigate(`/showcase/${slug}/product/${product.number}`)} aria-label={`${detailLabel}: ${product.name}`}><img src={product.photoUrls[0]} alt={product.name} loading="lazy" /></button><div className="e98-card-copy"><p>{product.category}</p><h3>{product.name}</h3><span>{product.variant}</span><p className="e98-card-description">{product.description}</p><strong>{product.price.toLocaleString(locale)} / {product.unit}</strong><div><button type="button" onClick={() => navigate(`/showcase/${slug}/product/${product.number}`)}>{detailLabel}</button>{whatsapp && !product.stockOut && <a href={`https://wa.me/${whatsapp}?text=${message}`} target="_blank" rel="noreferrer">{purchaseLabel}</a>}</div></div></article>; })}</div>; }
